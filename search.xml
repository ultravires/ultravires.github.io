<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title></title>
    <url>/2020/03/01/v2ray%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h2 id="购买服务器"><a href="#购买服务器" class="headerlink" title="购买服务器"></a>购买服务器</h2><h2 id="下载安装"><a href="#下载安装" class="headerlink" title="下载安装"></a>下载安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://install.direct/go.sh</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[root@vultrguest ~]<span class="comment"># sudo bash go.sh</span></span><br><span class="line">Installing V2Ray v4.22.1 on x86_64</span><br><span class="line">Downloading V2Ray: https://github.com/v2ray/v2ray-core/releases/download/v4.22.1/v2ray-linux-64.zip</span><br><span class="line">  % Total    % Received % Xferd  Average Speed   Time    Time     Time  Current</span><br><span class="line">                                 Dload  Upload   Total   Spent    Left  Speed</span><br><span class="line">100   608  100   608    0     0   3234      0 --:--:-- --:--:-- --:--:--  3234</span><br><span class="line">100 11.6M  100 11.6M    0     0  4169k      0  0:00:02  0:00:02 --:--:-- 5173k</span><br><span class="line">Archive:  /tmp/v2ray/v2ray.zip</span><br><span class="line">  inflating: /usr/bin/v2ray/geoip.dat</span><br><span class="line">  inflating: /usr/bin/v2ray/geosite.dat</span><br><span class="line">  inflating: /usr/bin/v2ray/v2ctl</span><br><span class="line">  inflating: /usr/bin/v2ray/v2ray</span><br><span class="line">PORT:29014</span><br><span class="line">UUID:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxx</span><br><span class="line">Archive:  /tmp/v2ray/v2ray.zip</span><br><span class="line">  inflating: /etc/systemd/system/v2ray.service</span><br><span class="line">Created symlink /etc/systemd/system/multi-user.target.wants/v2ray.service → /etc/systemd/system/v2ray.service.</span><br><span class="line">V2Ray v4.22.1 is installed.</span><br><span class="line">[root@vultrguest ~]<span class="comment">#</span></span><br></pre></td></tr></table></figure>
<h2 id="启动、停止、重启服务"><a href="#启动、停止、重启服务" class="headerlink" title="启动、停止、重启服务"></a>启动、停止、重启服务</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 启动</span></span><br><span class="line">systemctl start v2ray</span><br><span class="line"></span><br><span class="line"><span class="comment">## 停止</span></span><br><span class="line">systemctl stop v2ray</span><br><span class="line"></span><br><span class="line"><span class="comment">## 重启</span></span><br><span class="line">systemctl restart v2ray</span><br><span class="line"></span><br><span class="line"><span class="comment">## 开机自启</span></span><br><span class="line">systemctl <span class="built_in">enable</span> v2ray</span><br></pre></td></tr></table></figure>
<h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /etc/v2ray/config.json</span><br></pre></td></tr></table></figure>
<h2 id="开放端口"><a href="#开放端口" class="headerlink" title="开放端口"></a>开放端口</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 查看已开放端口</span></span><br><span class="line">firewall-cmd --zone=public --list-ports</span><br><span class="line"></span><br><span class="line"><span class="comment">## 添加开放端口</span></span><br><span class="line">firewall-cmd --zone=public --add-port=80/tcp --permanent</span><br></pre></td></tr></table></figure>
<h2 id="开关防火墙"><a href="#开关防火墙" class="headerlink" title="开关防火墙"></a>开关防火墙</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl stop firewalld</span><br><span class="line"></span><br><span class="line">systemctl start firewalld</span><br></pre></td></tr></table></figure>
<h2 id="修改时区"><a href="#修改时区" class="headerlink" title="修改时区"></a>修改时区</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime</span><br></pre></td></tr></table></figure>
<h2 id="重启服务器"><a href="#重启服务器" class="headerlink" title="重启服务器"></a>重启服务器</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">reboot</span><br></pre></td></tr></table></figure>
<h2 id="加速"><a href="#加速" class="headerlink" title="加速"></a>加速</h2><h2 id="一键部署-（以上步骤都不需要）"><a href="#一键部署-（以上步骤都不需要）" class="headerlink" title="一键部署 （以上步骤都不需要）"></a>一键部署 （以上步骤都不需要）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash &lt;(curl -s -L https://git.io/v2ray.sh)</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>设计模式（一）：观察者模式与发布/订阅模式</title>
    <url>/2019/12/15/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E8%AE%A2%E9%98%85%E5%8F%91%E5%B8%83%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p><strong>观察者模式（observer pattern）</strong> 和 <strong>发布/订阅模式（publish-subscribe pattern）</strong>。</p>
<a id="more"></a>
<h2 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Subject</span> </span>&#123;</span><br><span class="line">	<span class="keyword">constructor</span>() &#123;</span><br><span class="line">		<span class="keyword">this</span>.observers = [];</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	addObserver(observer) &#123;</span><br><span class="line">		<span class="keyword">this</span>.observers.push(observer);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	notify(params) &#123;</span><br><span class="line">		<span class="keyword">let</span> length = <span class="keyword">this</span>.observers.length;</span><br><span class="line">		<span class="keyword">while</span> (length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">this</span>.observers[--length].update(params);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Observer</span> </span>&#123;</span><br><span class="line">	update(params) &#123;</span><br><span class="line">		<span class="built_in">console</span>.log(params);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> observer1 = <span class="keyword">new</span> Observer();</span><br><span class="line"><span class="keyword">let</span> observer2 = <span class="keyword">new</span> Observer();</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> subject = <span class="keyword">new</span> Subject();</span><br><span class="line"></span><br><span class="line">subject.addObserver(observer1);</span><br><span class="line">subject.addObserver(observer2);</span><br><span class="line"></span><br><span class="line">subject.notify(<span class="string">'统一执行事件'</span>);</span><br></pre></td></tr></table></figure>
<h2 id="发布-订阅模式"><a href="#发布-订阅模式" class="headerlink" title="发布/订阅模式"></a>发布/订阅模式</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PubSub</span> </span>&#123;</span><br><span class="line">	<span class="keyword">constructor</span>() &#123;</span><br><span class="line">		<span class="keyword">this</span>.list = &#123;&#125;;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	subscribe(key, callback) &#123;</span><br><span class="line">		<span class="keyword">if</span> (!<span class="keyword">this</span>.list[key]) &#123;</span><br><span class="line">			<span class="keyword">this</span>.list[key] = [];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">this</span>.list[key].push(callback);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	unsubscribe(key, fn) &#123;</span><br><span class="line">		<span class="keyword">let</span> fns = <span class="keyword">this</span>.list[key];</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (!fns) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (!fn) &#123;</span><br><span class="line">			fns &amp;&amp; (fns.length = <span class="number">0</span>);</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">let</span> length = fns.length;</span><br><span class="line">			<span class="keyword">while</span>(length--) &#123;</span><br><span class="line">				fns[length] === fn ? fns.splice(length, <span class="number">1</span>) : <span class="string">''</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	publish(key, params) &#123;</span><br><span class="line">		<span class="keyword">let</span> fns = <span class="keyword">this</span>.list[key];</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (!fns || fns.length === <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">let</span> length = fns.length;</span><br><span class="line">		<span class="keyword">while</span> (length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">			fns[--length].apply(<span class="keyword">this</span>, params);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> pubSub = <span class="keyword">new</span> PubSub();</span><br><span class="line"></span><br><span class="line">pubSub.subscribe(<span class="string">'event'</span>, showMessage);</span><br><span class="line">pubSub.publish(<span class="string">'event'</span>, [<span class="string">"I'm Fish Chan."</span>]);</span><br><span class="line"></span><br><span class="line">pubSub.unsubscribe(<span class="string">'event'</span>, showMessage);</span><br><span class="line">pubSub.publish(<span class="string">'event'</span>, [<span class="string">"I'm ultravires."</span>]);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">showMessage</span>(<span class="params">message</span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(message);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>推荐阅读：</strong></p>
<p><a href="https://blog.csdn.net/itachi85/article/details/50773358" target="_blank" rel="noopener">设计模式（五）观察者模式，刘望舒</a></p>
]]></content>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6 学习笔记（三）：数组扩展</title>
    <url>/2019/10/11/ECMAScript6_%E6%95%B0%E7%BB%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h2 id="扩展运算符"><a href="#扩展运算符" class="headerlink" title="扩展运算符"></a>扩展运算符</h2><p><code>...</code> 为 <em>ES6</em> 中新增的 <em>扩展运算符</em> 。它可以将一个数组转换为用逗号分隔的参数序列。</p>
<a id="more"></a>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line"><span class="comment">// 同 console.log(1, 2, 3);</span></span><br></pre></td></tr></table></figure>
<p>利用这个特性我们可以很方便的将实现了 <code>Iterator</code> 的对象转为数组：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[...document.getElementsByTagName(<span class="string">'div'</span>)]</span><br><span class="line"><span class="comment">// [div, div, div, ...]</span></span><br></pre></td></tr></table></figure>
<p>扩展运算符还可以用来取代函数的 <code>apply</code> 方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ECMAScript5</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">x, y, z</span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(x + y + z); <span class="comment">// 6</span></span><br><span class="line">&#125;</span><br><span class="line">fn.apply(<span class="literal">null</span>, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ECMAScript6</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">x, y, z</span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(x + y + z); <span class="comment">// 6</span></span><br><span class="line">&#125;</span><br><span class="line">fn(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ... 的实际运用</span></span><br><span class="line"><span class="built_in">Math</span>.max(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]); <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Math</span>.max([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]); <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Math</span>.max.apply(<span class="literal">null</span>, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]); <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>
<p>其它实际运用：</p>
<p><strong>1. 克隆简单数组</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 克隆简单数组</span></span><br><span class="line"><span class="keyword">const</span> array1 = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="comment">// 写法一 (解构赋值)</span></span><br><span class="line"><span class="keyword">const</span> [...array2] = array1;</span><br><span class="line"><span class="comment">// 写法二 (扩展运算)</span></span><br><span class="line"><span class="keyword">const</span> array2 = [...array1];</span><br></pre></td></tr></table></figure>
<p>通过 <code>...</code> 运算符来拷贝数组的方法是浅拷贝，所以不能用来直接拷贝深数组。</p>
<p><strong>2. 合并数组</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> array1 = [<span class="number">1</span>, <span class="string">'a'</span>];</span><br><span class="line"><span class="keyword">const</span> array2 = [<span class="number">2</span>, <span class="string">'b'</span>];</span><br><span class="line"><span class="keyword">const</span> array3 = [<span class="number">3</span>, &#123; <span class="attr">c</span>: <span class="string">'c'</span> &#125;]; <span class="comment">// 应当避免此种拷贝</span></span><br><span class="line"><span class="keyword">const</span> array4 = [...array1, ...array2, ...array3];</span><br><span class="line"></span><br><span class="line">arra4[<span class="number">5</span>][<span class="string">'c'</span>] === arra3[<span class="number">1</span>][<span class="string">'c'</span>]; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p><strong>3. 将字符串转为数组</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[...<span class="string">'hello'</span>]</span><br><span class="line"><span class="comment">// [h, e, l, l, o]</span></span><br></pre></td></tr></table></figure>
<p>使用此方法可以正确识别四个字节的 Unicode 字符，可用于字符统计。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">'x\uD83D\uDE80y'</span>.length <span class="comment">// 4</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 注意前面的分号</span></span><br><span class="line">;[...<span class="string">'x\uD83D\uDE80y'</span>].length <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">length</span>(<span class="params">str</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> [...str].length;</span><br><span class="line">&#125;</span><br><span class="line">length(<span class="string">'x\uD83D\uDE80y'</span>) <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>
<p><strong>4. 将任意实现了 Iterator 接口的对象转为数组</strong></p>
<p>在写代码中我们经常遇到类数组不能使用 <code>forEach</code> 方法的问题，如：NodeList、HTMLCollection。之前我们做类型转换可能用到了:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ECMAScript5</span></span><br><span class="line">[].slice.call(HTMLCollection, <span class="literal">null</span>);</span><br></pre></td></tr></table></figure>
<p>或者 </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ECMAScript6</span></span><br><span class="line"><span class="built_in">Array</span>.from(HTMLCollection);</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">现在又多了一个方法：</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line">[...document.getElementsByTagName(<span class="string">'div'</span>)]</span><br></pre></td></tr></table></figure>
<p>上面的 <code>NodeList</code> 或 <code>HTMLCollection</code> 之所以可以使用扩展运算符将其转为真正的数组，原因就在于它们均实现了 <code>Iterator</code> 。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Number</span>.prototype[<span class="built_in">Symbol</span>.iterator] = <span class="function"><span class="keyword">function</span>*(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> i = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> num = <span class="keyword">this</span>.valueOf();</span><br><span class="line">  <span class="keyword">while</span> (i &lt; num) &#123;</span><br><span class="line">    <span class="keyword">yield</span> i++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log([..<span class="number">.5</span>]) <span class="comment">// [0, 1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure>
<h2 id="Array-from"><a href="#Array-from" class="headerlink" title="Array.from()"></a>Array.from()</h2><p><code>Array.from()</code> 同样来自 ECMAScript6，它与扩展运算符的用途很相似，但又有很大的不同。</p>
<h3 id="Array-from-和-…-的区别"><a href="#Array-from-和-…-的区别" class="headerlink" title="Array.from() 和 … 的区别"></a>Array.from() 和 … 的区别</h3><p><strong>Array.from：</strong> 该方法支持所有的类数组对象，所谓类数组对象就是具有 <code>length</code> 属性的所有对象。因此，只要有 <code>length</code> 属性的对象都可以通过 <code>Array.from</code> 方法转为数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Array</span>.from(&#123; <span class="attr">length</span>: <span class="number">3</span> &#125;);</span><br><span class="line"><span class="comment">// [ undefined, undefined, undefined ]</span></span><br></pre></td></tr></table></figure>
<p><code>Array.from</code> 方法还可以接受第二个参数，作用类似于数组的 <code>map</code> 方法，用来对每个元素进行处理，将处理后的值放入返回的数组中去。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Array</span>.from([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], (x) =&gt; x * x);</span><br><span class="line"><span class="comment">// [1, 4, 9]</span></span><br><span class="line"><span class="built_in">Array</span>.from([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]).map(<span class="function"><span class="params">x</span> =&gt;</span> x * x);</span><br><span class="line"><span class="comment">// [1, 4, 9]</span></span><br></pre></td></tr></table></figure>
<p><strong>扩展运算符(…):</strong> 扩展运算符背后调用的是遍历器接口（Symbol.iterator），如果一个对象没有部署这个接口，就无法转换。</p>
<h2 id="Array-of"><a href="#Array-of" class="headerlink" title="Array.of()"></a>Array.of()</h2><p><code>Array.of</code> 方法用于将一组值，转为数组，是扩展运算符的逆运算。它的出现弥补了数组构造函数 <code>Array()</code> 的不足。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 数组构造函数存在行为差异</span></span><br><span class="line"><span class="built_in">Array</span>() <span class="comment">// []</span></span><br><span class="line"><span class="built_in">Array</span>(<span class="number">3</span>) <span class="comment">// [, , ,]</span></span><br><span class="line"><span class="built_in">Array</span>(<span class="number">3</span>, <span class="number">11</span>, <span class="number">8</span>) <span class="comment">// [3, 11, 8]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Array.of 方法弥补了差异</span></span><br><span class="line"><span class="built_in">Array</span>.of(<span class="number">3</span>, <span class="number">11</span>, <span class="number">8</span>) <span class="comment">// [3,11,8]</span></span><br><span class="line"><span class="built_in">Array</span>.of(<span class="number">3</span>) <span class="comment">// [3]</span></span><br><span class="line"><span class="built_in">Array</span>.of(<span class="number">3</span>).length <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">Array</span>.of(<span class="literal">undefined</span>) <span class="comment">// [undefined]</span></span><br></pre></td></tr></table></figure>
<p>在 <em>ES5</em> 中可以用下面的方法模拟实现 <code>Array.of</code> 方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ArrayOf</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> [].slice.call(<span class="built_in">arguments</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>ECMAScript6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6学习笔记（二）：解构赋值</title>
    <url>/2019/10/07/ECMAScript6_%E8%A7%A3%E6%9E%84%E8%B5%8B%E5%80%BC/</url>
    <content><![CDATA[<p><em>ES6</em> 允许按照一定的模式从数组和对象中提取属性或值并将其赋值给其它变量，这就是 <em>解构(Destructuring)赋值</em> 。</p>
<a id="more"></a>
<h2 id="数组的解构赋值"><a href="#数组的解构赋值" class="headerlink" title="数组的解构赋值"></a>数组的解构赋值</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 完全解构</span></span><br><span class="line"><span class="keyword">let</span> [a, b, c] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="comment">// a:1, b:2, c:3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [, , c] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="comment">// c:3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a, ...c] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="comment">// a:1, c:[2, 3]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a, b, c] = [<span class="number">1</span>, <span class="number">2</span>]; <span class="comment">// a:1, b:2, c:undefined</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a, b, ...c] = [<span class="string">'a'</span>]; <span class="comment">// a:'a', b:undefined, c:[]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不完全解构</span></span><br><span class="line"><span class="keyword">let</span> [a, b] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="comment">// a:1, b:2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a, [b], c] = [<span class="number">1</span>, [<span class="number">2</span>, <span class="number">3</span>], <span class="number">4</span>];  <span class="comment">// a:1, b:2, c:4</span></span><br></pre></td></tr></table></figure>
<p>上面的写法可以看做是一种 <em>模式匹配</em> ，只要前后的模式一致，那么左边的变量就会被赋值成对应的值。</p>
<p>我们通常认为，<code>Set</code> 集合也是一种特殊的数组结构，所以，<code>Set</code> 也不例外的可以使用数组的解构赋值：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [x, y, z] = <span class="keyword">new</span> <span class="built_in">Set</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]); <span class="comment">// x:1, y:2, z:3</span></span><br></pre></td></tr></table></figure>
<p>实质上，只要某种数据结构具有 Iterator 接口就可以使用数组的解构赋值：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// (http://es6.ruanyifeng.com/#docs/destructuring)</span></span><br><span class="line"><span class="comment">// https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/function*</span></span><br><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">fibs</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> a = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> b = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">    <span class="keyword">yield</span> a;</span><br><span class="line">    [a, b] = [b, a + b];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [first, second, third, fourth, fifth, sixth] = fibs();</span><br><span class="line">sixth <span class="comment">// 5</span></span><br></pre></td></tr></table></figure>
<h3 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h3><p>为了防止从数组中取值得到是 <code>undefined</code> ，可以对表达式左侧的任意属性给予默认值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [a=<span class="number">1</span>] = []; <span class="comment">// a:1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a=<span class="number">1</span>] = [<span class="literal">undefined</span>]; <span class="comment">// a:1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a=<span class="number">1</span>] = [<span class="literal">null</span>]; <span class="comment">// a:null</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [x = <span class="number">1</span>, y = x] = [<span class="number">1</span>, <span class="number">2</span>]; <span class="comment">// x=1; y=2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [x = y, y = <span class="number">1</span>] = [];     <span class="comment">// ReferenceError: y is not defined</span></span><br></pre></td></tr></table></figure>
<p><em>ES6</em> 内部使用严格意义上的 <code>===</code> 进行比较，所以我们并不能避免得到的值为 <code>null</code> 。</p>
<p>如果默认值是一个表达式，那么这个表达式需要是 <em>惰性求值</em> 的，只有在用到的时候才求值：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'aaa'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [x = f()] = [<span class="literal">undefined</span>];</span><br><span class="line"><span class="comment">// 'aaa' 会被立即输出，而不需要调用 x();</span></span><br><span class="line"><span class="comment">// 如果将 x = f() 改为 x = f 则需要手动调用 x();</span></span><br></pre></td></tr></table></figure>
<h2 id="对象的解构赋值"><a href="#对象的解构赋值" class="headerlink" title="对象的解构赋值"></a>对象的解构赋值</h2><p>对象的解构与数组有一个重要的不同。数组的元素是按次序排列的，变量的取值由它的位置决定；而对象的属性没有次序，变量必须与属性同名，才能取到正确的值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; bar, foo &#125; = &#123; <span class="attr">foo</span>: <span class="string">'aaa'</span>, <span class="attr">bar</span>: <span class="string">'bbb'</span> &#125;;</span><br><span class="line">foo <span class="comment">// "aaa"</span></span><br><span class="line">bar <span class="comment">// "bbb"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123; baz &#125; = &#123; <span class="attr">foo</span>: <span class="string">'aaa'</span>, <span class="attr">bar</span>: <span class="string">'bbb'</span> &#125;;</span><br><span class="line">baz <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>
<p>对象的解构赋值，可以很方便地将现有对象的方法，赋值到某个变量：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; log &#125; = <span class="built_in">console</span>;</span><br><span class="line">log(<span class="string">'Goodbye~ The world!'</span>); <span class="comment">// 'Goodbye~ The world!'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> &#123; PI &#125; = <span class="built_in">Math</span>;</span><br><span class="line">PI; <span class="comment">// 3.141592653589793</span></span><br></pre></td></tr></table></figure>
<p>如果你想给变量一个不同于对象属性的名称，可以使用下面这种方式：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; <span class="attr">log</span>: echo &#125; = <span class="built_in">console</span>;</span><br><span class="line"></span><br><span class="line">echo(<span class="string">'Goodbye~ The world!'</span>); <span class="comment">// Goodbye~ The world!</span></span><br><span class="line"></span><br><span class="line">log(<span class="string">'Goodbye~ The world!'</span>); <span class="comment">// ReferenceError: log is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// &#123; foo, bar &#125; = &#123; foo: 'aaa', bar: 'bbb' &#125; 的本质</span></span><br><span class="line"><span class="comment">// 这归功于 ES6 对 对象的扩展</span></span><br><span class="line"><span class="keyword">let</span> &#123; <span class="attr">foo</span>: foo, <span class="attr">bar</span>: bar &#125; = &#123; <span class="attr">foo</span>: <span class="string">'aaa'</span>, <span class="attr">bar</span>: <span class="string">'bbb'</span> &#125;;</span><br></pre></td></tr></table></figure>
<p>下面看一个稍微复杂点的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> node = &#123;</span><br><span class="line">  loc: &#123;</span><br><span class="line">    start: &#123;</span><br><span class="line">      line: <span class="number">1</span>,</span><br><span class="line">      column: <span class="number">5</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123; loc, <span class="attr">loc</span>: &#123; start &#125;, <span class="attr">loc</span>: &#123; <span class="attr">start</span>: &#123; line &#125;&#125; &#125; = node;</span><br><span class="line">line <span class="comment">// 1</span></span><br><span class="line">loc  <span class="comment">// Object &#123;start: Object&#125;</span></span><br><span class="line">start <span class="comment">// Object &#123;line: 1, column: 5&#125;</span></span><br></pre></td></tr></table></figure>
<p>上面的代码中执行了多次解构，这样写的好处是我们能够很清晰的找到对象的层级解构。注意，这种写法中只有最内层 <code>{}</code> 包裹的能称之为变量，如 <code>loc: { start: { line } }</code> 中只有 <code>line</code> 是变量（严格意义上来说是： <code>line: line</code> 后面的 <code>line</code> 为变量），其它都为模式，即不能直接使用 <code>start</code>。</p>
<h3 id="默认值-1"><a href="#默认值-1" class="headerlink" title="默认值"></a>默认值</h3><p>对象的解构赋值也可以基于默认值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> &#123;x = <span class="number">3</span>&#125; = &#123;&#125;;</span><br><span class="line">x <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123;x, y = <span class="number">5</span>&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;;</span><br><span class="line">x <span class="comment">// 1</span></span><br><span class="line">y <span class="comment">// 5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123;<span class="attr">x</span>: y = <span class="number">3</span>&#125; = &#123;&#125;;</span><br><span class="line">y <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123;<span class="attr">x</span>: y = <span class="number">3</span>&#125; = &#123;<span class="attr">x</span>: <span class="number">5</span>&#125;;</span><br><span class="line">y <span class="comment">// 5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123; <span class="attr">message</span>: msg = <span class="string">'Something went wrong'</span> &#125; = &#123;&#125;;</span><br><span class="line">msg <span class="comment">// "Something went wrong"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123;x = <span class="number">3</span>&#125; = &#123;<span class="attr">x</span>: <span class="literal">undefined</span>&#125;;</span><br><span class="line">x <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> &#123;x = <span class="number">3</span>&#125; = &#123;<span class="attr">x</span>: <span class="literal">null</span>&#125;;</span><br><span class="line">x <span class="comment">// null</span></span><br></pre></td></tr></table></figure>
<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><p>将 <code>{}</code> 写在行首并不构成表达式，而是一个代码块。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误的写法</span></span><br><span class="line"><span class="keyword">let</span> x;</span><br><span class="line">&#123;x&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;;</span><br><span class="line"><span class="comment">// SyntaxError: syntax error</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确的写法</span></span><br><span class="line"><span class="keyword">let</span> x;</span><br><span class="line">(&#123;x&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;);</span><br></pre></td></tr></table></figure>
<p>数组本质是特殊的对象，因此可以对数组进行对象属性的解构。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">let</span> &#123;<span class="number">0</span> : first, [arr.length - <span class="number">1</span>] : last&#125; = arr;</span><br><span class="line">first <span class="comment">// 1</span></span><br><span class="line">last <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>
<h2 id="字符串的解构赋值"><a href="#字符串的解构赋值" class="headerlink" title="字符串的解构赋值"></a>字符串的解构赋值</h2><p>字符串也可以解构赋值。这是因为此时，字符串被转换成了一个类似数组的对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [h, e, l, l2, o] = <span class="string">'hello'</span>;</span><br><span class="line">h <span class="comment">// "h"</span></span><br><span class="line">e <span class="comment">// "e"</span></span><br><span class="line">l <span class="comment">// "l"</span></span><br><span class="line">l2 <span class="comment">// 'l' // 变量名不能同为 'l', 否则 SyntaxError: Identifier 'l' has already been declared</span></span><br><span class="line">o <span class="comment">// "o"</span></span><br></pre></td></tr></table></figure>
<p>类似数组的对象都有一个length属性，因此还可以对这个属性解构赋值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;<span class="attr">length</span> : len&#125; = <span class="string">'hello'</span>;</span><br><span class="line">len <span class="comment">// 5</span></span><br></pre></td></tr></table></figure>
<h2 id="数值和布尔值的解构赋值"><a href="#数值和布尔值的解构赋值" class="headerlink" title="数值和布尔值的解构赋值"></a>数值和布尔值的解构赋值</h2><p>前面说解构赋值用于从数组和对象中提取属性或值并将其赋值给其它变量，如果解构表达式的右值为数字或布尔类型的时候内部会先将其转换为对象：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; toString &#125; = <span class="number">9</span>;</span><br><span class="line">toString === <span class="built_in">Number</span>.prototype.toString; <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123; toString &#125; = <span class="literal">true</span>;</span><br><span class="line">toString === <span class="built_in">Boolean</span>.protype.toString; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>由于 <code>undefined</code> 和 <code>null</code> 不能被转换为对象（虽然 null 通常被视作一个对象，但是它是空对象，不具有任何属性），所以对它们进行解构赋值会报错：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// TypeError: Cannot destructure property `prop` of 'undefined' or 'null'.</span></span><br><span class="line"><span class="keyword">let</span> &#123; toString &#125; = <span class="literal">undefined</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// TypeError: Cannot destructure property `prop` of 'undefined' or 'null'.</span></span><br><span class="line"><span class="keyword">let</span> &#123; toString &#125; = <span class="literal">null</span>;</span><br></pre></td></tr></table></figure>
<h2 id="函数参数的解构赋值"><a href="#函数参数的解构赋值" class="headerlink" title="函数参数的解构赋值"></a>函数参数的解构赋值</h2><p>函数的实参和形参之间也可以进行模式匹配：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">[x, y]</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br><span class="line">add([<span class="number">2</span>, <span class="number">3</span>]); <span class="comment">// 5</span></span><br><span class="line"></span><br><span class="line">[[<span class="number">1</span>,<span class="number">2</span>], [<span class="number">3</span>,<span class="number">4</span>]].map(<span class="function">(<span class="params">[a, b]</span>) =&gt;</span> a + b); <span class="comment">// [3, 7]</span></span><br></pre></td></tr></table></figure>
<p>函数参数的解构也可以有默认值：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">moveTo</span>(<span class="params">&#123;x = <span class="number">0</span>, y = <span class="number">0</span>&#125; = &#123;&#125;</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> &#123;x, y&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">moveTo(&#123; <span class="attr">x</span>: <span class="number">10</span>, <span class="attr">y</span>: <span class="number">2</span> &#125;);<span class="comment">// &#123;x: 10, y: 2&#125;</span></span><br><span class="line">moveTo(&#123; <span class="attr">y</span>: <span class="number">3</span> &#125;); <span class="comment">// &#123;x: 0, y: 3&#125;</span></span><br><span class="line">moveTo(&#123; &#125;); <span class="comment">// &#123;x: 0, y: 0&#125;</span></span><br><span class="line">moveTo(); <span class="comment">// &#123;x: 0, y: 0&#125;</span></span><br></pre></td></tr></table></figure>
<p>但是如果你按照下面的写法给函数参数的解构基于默认值就会出现问题：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">move</span>(<span class="params">&#123;x, y&#125; = &#123; x: <span class="number">3</span>, y: <span class="number">2</span> &#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> [x, y];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">move(&#123;<span class="attr">x</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">8</span>&#125;); <span class="comment">// [3, 8]</span></span><br><span class="line">move(&#123;<span class="attr">x</span>: <span class="number">3</span>&#125;); <span class="comment">// [3, undefined]</span></span><br><span class="line">move(&#123;&#125;); <span class="comment">// [undefined, undefined]</span></span><br><span class="line">move(); <span class="comment">// [3, 2]</span></span><br></pre></td></tr></table></figure>
<p>实际上这个问题很好理解：如果函数的实参不为空，则会替换函数形参的右值。那么这里的 <code>move({x: 3})</code> 类似于 <code>function move({x, y} = {x: 3})</code>，这下读出 <code>{x, y} = {x: 3})</code> 的结果为 <code>x: 3, y: undefined</code> 就很容易了。</p>
<h2 id="圆括号问题"><a href="#圆括号问题" class="headerlink" title="圆括号问题"></a>圆括号问题</h2><p>当解构赋值语句中包含小括号 <code>()</code> 的时候是很容易出错的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全部报错</span></span><br><span class="line"><span class="keyword">let</span> [(a)] = [<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123;<span class="attr">x</span>: (c)&#125; = &#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> (&#123;<span class="attr">x</span>: c&#125;) = &#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> &#123;(x: c)&#125; = &#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> &#123;(x): c&#125; = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123; <span class="attr">o</span>: (&#123; <span class="attr">p</span>: p &#125;) &#125; = &#123; <span class="attr">o</span>: &#123; <span class="attr">p</span>: <span class="number">2</span> &#125; &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 报错</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">[(z</span>)]) </span>&#123; <span class="keyword">return</span> z; &#125;</span><br><span class="line"><span class="comment">// 报错</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">[z,(x</span>)]) </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 全部报错</span></span><br><span class="line">(&#123; <span class="attr">p</span>: a &#125;) = &#123; <span class="attr">p</span>: <span class="number">42</span> &#125;;</span><br><span class="line">([a]) = [<span class="number">5</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 报错</span></span><br><span class="line">[(&#123; <span class="attr">p</span>: a &#125;), &#123; <span class="attr">x</span>: c &#125;] = [&#123;&#125;, &#123;&#125;];</span><br></pre></td></tr></table></figure>
<p>只有圆括号所包含的内容不是模式的一部分且非声明式语句的时候才允许被使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[(b)] = [<span class="number">3</span>]; <span class="comment">// 正确</span></span><br><span class="line">(&#123; <span class="attr">p</span>: (d) &#125; = &#123;&#125;); <span class="comment">// 正确</span></span><br><span class="line">[(<span class="built_in">parseInt</span>.prop)] = [<span class="number">3</span>]; <span class="comment">// 正确</span></span><br></pre></td></tr></table></figure>
<h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><ol>
<li>交换变量的值</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> y = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">[x, y] = [y, x];</span><br></pre></td></tr></table></figure>
<ol start="2">
<li>从函数返回多个值并获取</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [a, b, c] = fn();</span><br></pre></td></tr></table></figure>
<ol start="3">
<li>从对象中获取需要的属性</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> user = &#123; <span class="attr">name</span>: <span class="string">'ultravires'</span>, <span class="attr">age</span>: <span class="number">12</span>, <span class="attr">email</span>: <span class="string">'x555666777@qq.com'</span> &#125;;</span><br><span class="line"><span class="keyword">let</span> &#123;name, email&#125; = user;</span><br></pre></td></tr></table></figure>
<p>这在处理 JSON 数据的时候尤其有用，这也是解构赋值最方便的应用，它是大多数解构赋值应用的根本。</p>
<ol start="4">
<li>将所有的类数组对象转为数组</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> nodes = <span class="built_in">document</span>.getElementsByTagName(<span class="string">'div'</span>);</span><br><span class="line">[...nodes].forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123; <span class="built_in">console</span>.log(item); &#125;);</span><br></pre></td></tr></table></figure>
<ol start="5">
<li>输入模块的指定方法</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; SourceMapConsumer, SourceNode &#125; = <span class="built_in">require</span>(<span class="string">"source-map"</span>);</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>ECMAScript6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6学习笔记（一）：变量声明</title>
    <url>/2019/10/06/ECMAScript6_%E5%8F%98%E9%87%8F%E5%A3%B0%E6%98%8E/</url>
    <content><![CDATA[<p><code>ES6</code> 添加了 <code>let</code> 和<code>const</code> 命令，还有另外两种声明变量的方法：<code>import</code> 命令和 <code>class</code> 命令。所以，<em>ES6</em> 一共有 6 种声明变量的方法。</p>
<a id="more"></a>
<h2 id="let"><a href="#let" class="headerlink" title="let"></a>let</h2><p><code>let</code> 命令用来声明具有 <em>块级作用域</em> 的变量。通常，我们把包含在 <code>{}</code> 内的代码就称为一个代码块。</p>
<h3 id="暂时性死区（TDZ）"><a href="#暂时性死区（TDZ）" class="headerlink" title="暂时性死区（TDZ）"></a>暂时性死区（TDZ）</h3><p><em>暂时性死区（TDZ, temporal dead zone）</em> 主要是为了减少运行时错误，防止在变量声明前就使用这个变量，从而导致意料之外的行为。这样的错误在 ES5 是很常见的，现在有了这种规定，避免此类错误就很容易了。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">let</span> i = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (i; i &lt; <span class="number">3</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(i); <span class="comment">// Thrown: ReferenceError: i is not defined</span></span><br><span class="line">		<span class="keyword">let</span> i = <span class="number">2</span>;</span><br><span class="line">		<span class="built_in">console</span>.log(i);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>
<p>而 _c_ 语言中并不会存在暂时性死区的问题，原因是 _c_ 语言中不同作用域中声明的变量所分配的地址不同。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* @file test.c</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (i; i &lt; <span class="number">3</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"i_out = %d"</span>, i); <span class="comment">// 0, 1, 2</span></span><br><span class="line">		<span class="keyword">int</span> i = <span class="number">8</span>;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"i_in = %d"</span>, i); <span class="comment">// 8, 8, 8</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 程序正常执行，执行结果请自行编译运行。</span></span><br></pre></td></tr></table></figure>
<h3 id="不允许重复声明"><a href="#不允许重复声明" class="headerlink" title="不允许重复声明"></a>不允许重复声明</h3><p><code>let</code> 命令不允许在相同作用域内声明同名变量，_c_ 语言中也是如此。</p>
<h3 id="不存在变量提升"><a href="#不存在变量提升" class="headerlink" title="不存在变量提升"></a>不存在变量提升</h3><p>ES6 中规定在暂时性死区中和用 let、const 所声明的变量<strong>不</strong>存在 <em>变量提升</em>。</p>
<h2 id="const"><a href="#const" class="headerlink" title="const"></a>const</h2><p><code>const</code> 声明一个只读常量，一旦声明，值就不允许被修改。所以，我们在使用 <code>const</code> 命令进行变量的时候就需要立即对该变量进行初始化，否则会报错：<code>Uncaught SyntaxError: Missing initializer in const declaration</code> 。</p>
<p><code>const</code> 命令声明的常量也不提升，同样存在暂时性死区，变量只能在声明之后使用。</p>
<h3 id="不能变的是地址指向，而不是值"><a href="#不能变的是地址指向，而不是值" class="headerlink" title="不能变的是地址指向，而不是值"></a>不能变的是地址指向，而不是值</h3><p><code>const</code> 实质上并不保证所声明变量的值不能改变，不能被改变的是该变量的地址所存储的值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">const</span> b = a;</span><br><span class="line"></span><br><span class="line">a = <span class="number">4</span>; <span class="comment">// 这里改变 a 的值并不会报错</span></span><br></pre></td></tr></table></figure>
<h2 id="ES6-中的块级作用域"><a href="#ES6-中的块级作用域" class="headerlink" title="ES6 中的块级作用域"></a>ES6 中的块级作用域</h2><p>ES6 中的 <code>let</code> 和 <code>const</code> 为 <em>JavaScript</em> 提供了块级作用域。块级作用域的出现使得 IIFE 写法不再必要:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// IIFE</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">var</span> a = <span class="number">2</span>;</span><br><span class="line">&#125;)();</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">// 'a' is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 块级作用域</span></span><br><span class="line">&#123; <span class="keyword">let</span> a = <span class="number">2</span>; &#125;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">// 'a' is not defined</span></span><br></pre></td></tr></table></figure>
<h2 id="ES6-声明变量的六种方法"><a href="#ES6-声明变量的六种方法" class="headerlink" title="ES6 声明变量的六种方法"></a>ES6 声明变量的六种方法</h2><p><em>ES5</em> 只有两种声明变量的方法：<code>var</code> 命令和 <code>function</code> 命令。<code>ES6</code> 除了添加 <code>let</code> 和<code>const</code> 命令，还有另外两种声明变量的方法：<code>import</code> 命令和 <code>class</code> 命令。所以，<em>ES6</em> 一共有 6 种声明变量的方法。</p>
<h1 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h1><blockquote>
<p><a href="http://es6.ruanyifeng.com/" target="_blank" rel="noopener">ECMAScript6 入门，阮一峰</a></p>
</blockquote>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>ECMAScript6</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows常见错误</title>
    <url>/2019/05/17/Windows%E5%B8%B8%E8%A7%81%E9%94%99%E8%AF%AF/</url>
    <content><![CDATA[<p>此文章记录了本人在使用 Windows 操作系统中所遇到的一些问题，文章的篇幅会随着个人遇到的问题越来越长。</p>
<a id="more"></a>
<h2 id="使用对象的权限被否定"><a href="#使用对象的权限被否定" class="headerlink" title="使用对象的权限被否定"></a>使用对象的权限被否定</h2><p><strong>原因：</strong>安装了office控件，但该控件没有权限。如：AxMath</p>
<p><strong>解决办法：</strong>删除此控件或赋予该控件管理员权限</p>
<p><strong>如何找：</strong></p>
<ol>
<li>打开注册表</li>
<li>计算机\HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\Microsoft\Office</li>
<li>找到你的office目录后点击响应的office产品查看其装有的控件</li>
</ol>
<p><img src="https://i.loli.net/2019/05/17/5cde011d58dca77583.png" alt="注册表"></p>
<p>我这里PowerPoint下未装任何控件（AxMath已被我卸载）。如果你有，请自行寻找安装的控件位置进行赋权或卸载操作。</p>
]]></content>
      <categories>
        <category>捉BUG</category>
      </categories>
      <tags>
        <tag>windows</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL：validate_password</title>
    <url>/2019/03/22/MySQL%EF%BC%9Avalidate_password/</url>
    <content><![CDATA[<p>今天在给 MySQL 开启远程权限的时候报错了： </p>
<blockquote>
<p>your password does not satisfy the current policy requirements.</p>
</blockquote>
<p>经查阅相关资料发现，现在安装 <code>MySQL 5.6</code> 便会给你自动安装一个密码验证插件 <a href="https://dev.mysql.com/doc/refman/5.6/en/validate-password.html" target="_blank" rel="noopener">validate_plugin</a>，不知道是这个插件本身存在 bug 还是我的操作有问题，不管我怎么设置其插件的参数在重启服务后该插件的参数始终会被重置。在网上也查阅了相关设置参数的和关闭插件的方法，其做法都大相径庭，但是“然并卵”！。</p>
<p>索性就将其卸载了（<a href="https://blog.csdn.net/kuluzs/article/details/51924374" target="_blank" rel="noopener">修改参数的方法见此</a>， 也可以参考<a href="https://dev.mysql.com/doc/refman/5.6/en/validate-password.html" target="_blank" rel="noopener">官网</a>的介绍）。</p>
<a id="more"></a>
<h2 id="卸载-validate-password-插件"><a href="#卸载-validate-password-插件" class="headerlink" title="卸载 validate_password 插件"></a>卸载 validate_password 插件</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; uninstall plugin validate_password;</span><br><span class="line">Query OK, 0 rows affected (0.05 sec)</span><br></pre></td></tr></table></figure>
<h2 id="开启远程访问权限"><a href="#开启远程访问权限" class="headerlink" title="开启远程访问权限"></a>开启远程访问权限</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; GRANT ALL PRIVILEGES ON *.* TO &apos;root&apos;@&apos;%&apos; IDENTIFIED BY &apos;123456&apos; WITH GRANT OPTION;</span><br><span class="line"></span><br><span class="line">mysql&gt; FLUSH PRIVILEGES;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>推荐系统（二）：协同过滤</title>
    <url>/2019/03/19/%E6%8E%A8%E8%8D%90%E7%B3%BB%E7%BB%9F%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E5%8D%8F%E5%90%8C%E8%BF%87%E6%BB%A4/</url>
    <content><![CDATA[<p>上一篇文章我整理了一些常见的智能推荐算法，这一次我将更加详细的讲解<strong>协同过滤：基于用户的协同过滤（User-Based Collaborative）和基于项目的协同过滤（Item-Based Collaborative）</strong>。</p>
<a id="more"></a>
<h2 id="基于用户的协同过滤（User-Based-Collaborative）"><a href="#基于用户的协同过滤（User-Based-Collaborative）" class="headerlink" title="基于用户的协同过滤（User-Based Collaborative）"></a>基于用户的协同过滤（User-Based Collaborative）</h2><p>假设你想推荐一部电影给你的用户 Mark，但是我们怎么能确定我推荐的电影能让 Mark 喜欢呢？我们总不能去问：嘿！Mark 你喜欢看《成龙历险记》吗？这样愚蠢的问题吧。如果他能回答你，就说明他早已经看过了，就没有推荐的必要了。但是如果你知道还有一个用户 Jackson 与 Mark 具有相似的爱好，如他们都看过《喜羊羊与灰太狼》、《熊出没》和《大头儿子小头爸爸》且都给了很高的评价。而 Jackson 还看过了《大耳朵图图》且给了很高的评分，Mark 却没有看过。那么我们可以猜想 Mark 可能也喜欢《大耳朵图图》，并把此动画片推荐给 Mark。这就是简单的基于用户的协同过滤的例子。</p>
<p>那么，将这个问题放到数学的思想上来我们应该怎么做呢？</p>
<p>首先，我们需要这样一个矩阵，它包含了 <strong>User-Item-Rating</strong> 。</p>
<table>
<thead>
<tr>
<th style="text-align:center"></th>
<th style="text-align:center">商品1</th>
<th style="text-align:center">商品2</th>
<th style="text-align:center">商品3</th>
<th style="text-align:center">商品4</th>
<th style="text-align:center">商品5</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">用户1</td>
<td style="text-align:center">1</td>
<td style="text-align:center">-1</td>
<td style="text-align:center">1</td>
<td style="text-align:center">1</td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center">用户2</td>
<td style="text-align:center"></td>
<td style="text-align:center">1</td>
<td style="text-align:center"></td>
<td style="text-align:center"></td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center">用户3</td>
<td style="text-align:center">1</td>
<td style="text-align:center">-1</td>
<td style="text-align:center">1</td>
<td style="text-align:center"></td>
<td style="text-align:center">1</td>
</tr>
<tr>
<td style="text-align:center">用户4</td>
<td style="text-align:center">-1</td>
<td style="text-align:center">-1</td>
<td style="text-align:center">1</td>
<td style="text-align:center">1</td>
<td style="text-align:center">1</td>
</tr>
<tr>
<td style="text-align:center">用户5</td>
<td style="text-align:center">-1</td>
<td style="text-align:center">-1</td>
<td style="text-align:center"></td>
<td style="text-align:center">1</td>
<td style="text-align:center">1</td>
</tr>
</tbody>
</table>
<p>1) 有了这张表，我们就可以通过一些算法来计算用户的相似性，如 KNN 邻近算法：Similarity(a, i) = w(a, i), i ∈ K。</p>
<p>2) 找到 a 的相似用户后，我们就可以根据 K 个相似用户的喜好来向用户 a 推荐产品了。</p>
<h2 id="基于项目的协同过滤（Item-Based-Collaborative）"><a href="#基于项目的协同过滤（Item-Based-Collaborative）" class="headerlink" title="基于项目的协同过滤（Item-Based Collaborative）"></a>基于项目的协同过滤（Item-Based Collaborative）</h2><p>与基于用户的协同过滤类似，只是我们关注的对象发生了转变。基于用户的协同过滤是计算用户的相似性，基于项目的协同过滤就是计算项目的相似性了。</p>
<p>我们都访问过电商平台，如淘宝。淘宝就有基于项目的协同过滤推荐系统。你搜索了“电脑”，淘宝就会给你推荐键盘、鼠标等相关产品。这就典型的基于项目的协同过滤的应用。</p>
]]></content>
      <categories>
        <category>推荐系统</category>
      </categories>
      <tags>
        <tag>推荐系统</tag>
        <tag>协同过滤</tag>
      </tags>
  </entry>
  <entry>
    <title>npm 源管理</title>
    <url>/2019/03/12/npm%E6%BA%90%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p><code>nrm</code> 是一个 NPM  源管理器，允许你快速切换 NPM 源。</p>
<a id="more"></a>
<h3 id="Install"><a href="#Install" class="headerlink" title="Install"></a>Install</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g nrm</span><br></pre></td></tr></table></figure>
<h3 id="How"><a href="#How" class="headerlink" title="How"></a>How</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PS E:\毕业项目\sanphantom&gt; nrm ls</span><br><span class="line"></span><br><span class="line">  npm ---- https://registry.npmjs.org/</span><br><span class="line">  cnpm --- http://r.cnpmjs.org/</span><br><span class="line">* taobao - https://registry.npm.taobao.org/</span><br><span class="line">  nj ----- https://registry.nodejitsu.com/</span><br><span class="line">  npmMirror  https://skimdb.npmjs.com/registry/</span><br><span class="line">  edunpm - http://registry.enpmjs.org/</span><br><span class="line">  </span><br><span class="line">PS E:\毕业项目\sanphantom&gt; nrm use cnpm</span><br><span class="line"></span><br><span class="line">   Registry has been <span class="built_in">set</span> to: http://r.cnpmjs.org/</span><br></pre></td></tr></table></figure>
<p>增加源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nrm add &lt;registry&gt; &lt;url&gt; [home]</span><br></pre></td></tr></table></figure>
<p>删除源：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nrm del &lt;registry&gt;</span><br></pre></td></tr></table></figure>
<p>测试速度：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nrm <span class="built_in">test</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>npm</category>
      </categories>
      <tags>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>推荐系统（一）：常用的智能推荐算法</title>
    <url>/2019/03/11/%E6%8E%A8%E8%8D%90%E7%B3%BB%E7%BB%9F%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E5%B8%B8%E7%94%A8%E7%9A%84%E6%99%BA%E8%83%BD%E6%8E%A8%E8%8D%90%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>公司最近需要研究智能推荐算法，技术总监在群里要投名状。我一个前端不知道是哪根筋短路了，竟脑子一热，自送断头台。没办法，既然递过了投名状那就硬着头皮去做好了。</p>
<a id="more"></a>
<h2 id="基于内容推荐"><a href="#基于内容推荐" class="headerlink" title="基于内容推荐"></a>基于内容推荐</h2><p>基于内容的信息推荐方法的理论依据主要来自于<a href="https://baike.baidu.com/item/%E4%BF%A1%E6%81%AF%E6%A3%80%E7%B4%A2" target="_blank" rel="noopener"><strong>信息检索</strong></a>和<a href="https://baike.baidu.com/item/%E4%BF%A1%E6%81%AF%E8%BF%87%E6%BB%A4" target="_blank" rel="noopener"><strong>信息过滤</strong></a>，所谓的基于内容的推荐方法就是根据用户过去的浏览记录来向用户推荐用户没有接触过的推荐项。主要是从两个方法来描述基于内容的推荐方法：启发式的方法和基于模型的方法。启发式的方法就是用户凭借经验来定义相关的计算公式，然后再根据公式的计算结果和实际的结果进行验证，然后再不断修改公式以达到最终目的。而对于模型的方法就是根据以往的数据作为数据集，然后根据这个数据集来学习出一个模型。一般的推荐系统中运用到的启发式的方法就是使用<a href="https://baike.baidu.com/item/tf-idf" target="_blank" rel="noopener"><strong>tf-idf</strong></a>的方法来计算，跟还有tf-idf的方法计算出这个文档中出现权重比较高的关键字作为描述用户特征，并使用这些关键字作为描述用户特征的向量；然后再根据被推荐项中的权重高的关键字来作为推荐项的属性特征，然后再将这个两个向量最相近的（与用户特征的向量计算得分最高）的项推荐给用户。在计算用户特征向量和被推荐项的特征向量的相似性时，一般使用的是cosine方法，计算两个向量之间夹角的cosine值。</p>
<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>推荐结果直观，容易解释</li>
<li>不需要领域知识</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>新用户问题</li>
<li>复杂属性不好处理</li>
<li>要有足够数据构造分类器</li>
</ul>
<h2 id="协同过滤（Collaborative-Filtering-CF）"><a href="#协同过滤（Collaborative-Filtering-CF）" class="headerlink" title="协同过滤（Collaborative Filtering, CF）"></a>协同过滤（Collaborative Filtering, CF）</h2><p><strong>协同过滤（Collaborative Filtering, CF）</strong>，简单来说就是通过与你相似用户的喜好向你推荐相关产品。就好比，你在网上有很多“知己”，你们的兴趣爱好大致相同，那么如果我知道了其中一位“知己”的兴趣爱好就可以推断你可能也喜欢某些东西。协同过滤不一定局限于特别感兴趣的，特别不敢兴趣的记录也十分重要。</p>
<h3 id="基于用户（User-based）的协同过滤"><a href="#基于用户（User-based）的协同过滤" class="headerlink" title="基于用户（User-based）的协同过滤"></a>基于用户（User-based）的协同过滤</h3><p>用<strong>相似统计</strong>的方法得到具有相似爱好或者兴趣的同类用户，根据同类用户的喜好预测目标用户对某个物品的喜好程度，然后找到评分最高的若干物品推荐给目标用户。（找相似用户）</p>
<h3 id="基于项目（Item-based）的协同过滤"><a href="#基于项目（Item-based）的协同过滤" class="headerlink" title="基于项目（Item-based）的协同过滤"></a>基于项目（Item-based）的协同过滤</h3><p>根据目标用户对物品的历史评分，找到相似物品，并将目标用户可能喜欢的若干物品推荐给他。（找相似物品）</p>
<h3 id="基于模型（Model-based）的协同过滤"><a href="#基于模型（Model-based）的协同过滤" class="headerlink" title="基于模型（Model-based）的协同过滤"></a>基于模型（Model-based）的协同过滤</h3><p>基于模型的协同过滤作为目前最主流的协同过滤类型，其相关算法可以写一本书了，当然我们这里主要是对其思想做有一个归类概括。我们的问题是这样的m个物品，m个用户的数据，只有部分用户和部分数据之间是有评分数据的，其它部分评分是空白，此时我们要用已有的部分稀疏数据来预测那些空白的物品和数据之间的评分关系，找到最高评分的物品推荐给用户。</p>
<p>对于这个问题，用机器学习的思想来建模解决，主流的方法可以分为：用关联算法，聚类算法，分类算法，回归算法，矩阵分解，神经网络,图模型以及隐语义模型来解决。</p>
<h3 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h3><p>以用户的角度来推荐的协同过滤系统有以下优点：</p>
<ul>
<li>能够过滤机器难以自动内容分析的信息。如艺术品、音乐等。</li>
<li>共享其他人的经验，避免了内容分析的不完全或不精确，并且能够基于一些复杂的，难以表述的概念（如信息质量、个人品味）进行过滤。</li>
<li>有推荐新信息的能力，可以发现内容上完全不相似的信息，用户对推荐信息的内容是事先预料不到的。可以发现用户潜在的但自己未发现的兴趣偏好。</li>
<li>推荐个性化、自动化程度高。能够有效的利用其他相似用户的反馈信息加快个性化学习的速度。</li>
</ul>
<h3 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h3><p>虽然协同过滤作为一推荐机制有其相当的应用，但协同过滤仍有许多的问题需要解决。整体而言，最典型的问题有：</p>
<ul>
<li>新用户问题，系统开始时推荐质量较差</li>
<li>新项目问题，质量取决于历史数据集</li>
<li>稀疏性问题</li>
<li>系统延伸性问题</li>
</ul>
<h2 id="基于规则推荐"><a href="#基于规则推荐" class="headerlink" title="基于规则推荐"></a>基于规则推荐</h2><p>基于关联规则的推荐（Association Rule-based Recommendation）是以关联规则为基础，把已购商品作为规则头，规则体为推荐对象。关联规则挖掘可以发现不同商品在销售过程中的相关性，在零售业中已经得到了成功的应用。关联规则就是在一个交易数据库中统计购买了商品集X的交易中有多大比例的交易同时购买了商品集Y，其直观的意义就是用户在购买某些商品的时候有多大倾向去购买另外一些商品。比如购买牛奶的同时很多人会同时购买面包。</p>
<p>算法的第一步关联规则的发现最为关键且最耗时，是算法的瓶颈，但可以离线进行。其次，商品名称的同义性问题也是关联规则的一个难点。</p>
<h2 id="基于效用推荐"><a href="#基于效用推荐" class="headerlink" title="基于效用推荐"></a>基于效用推荐</h2><p>基于效用的推荐（Utility-based Recommendation）是建立在对用户使用项目的效用情况上计算的，其核心问题是怎么样为每一个用户去创建一个效用函数，因此，用户资料模型很大程度上是由系统所采用的效用函数决定的。基于效用推荐的好处是它能把非产品的属性，如提供商的可靠性（Vendor Reliability）和产品的可得性（Product Availability）等考虑到效用计算中。</p>
<h2 id="基于知识推荐"><a href="#基于知识推荐" class="headerlink" title="基于知识推荐"></a>基于知识推荐</h2><p>基于知识的推荐（Knowledge-based Recommendation）在某种程度是可以看成是一种推理（Inference）技术，它不是建立在用户需要和偏好基础上推荐的。基于知识的方法因它们所用的功能知识不同而有明显区别。效用知识（Functional Knowledge）是一种关于一个项目如何满足某一特定用户的知识，因此能解释需要和推荐的关系，所以用户资料可以是任何能支持推理的知识结构，它可以是用户已经规范化的查询，也可以是一个更详细的用户需要的表示。</p>
<h2 id="组合推荐"><a href="#组合推荐" class="headerlink" title="组合推荐"></a>组合推荐</h2><p>由于各种推荐方法都有优缺点，所以在实际中，组合推荐（Hybrid Recommendation）经常被采用。研究和应用最多的是内容推荐和协同过滤推荐的组合。最简单的做法就是分别用基于内容的方法和协同过滤推荐方法去产生一个推荐预测结果，然后用某方法组合其结果。尽管从理论上有很多种推荐组合方法，但在某一具体问题中并不见得都有效，组合推荐一个最重要原则就是通过组合后要能避免或弥补各自推荐技术的弱点。</p>
<p>在组合方式上，有研究人员提出了七种组合思路：<br>　　1）<a href="https://baike.baidu.com/item/%E5%8A%A0%E6%9D%83" target="_blank" rel="noopener">加权</a>（Weight）：加权多种推荐技术结果。<br>　　2）<a href="https://baike.baidu.com/item/%E5%8F%98%E6%8D%A2" target="_blank" rel="noopener">变换</a>（Switch）：根据问题背景和实际情况或要求决定变换采用不同的推荐技术。<br>　　3）<a href="https://baike.baidu.com/item/%E6%B7%B7%E5%90%88" target="_blank" rel="noopener">混合</a>（Mixed）：同时采用多种推荐技术给出多种推荐结果为用户提供参考。<br>　　4）特征组合（Feature combination）：组合来自不同推荐数据源的特征被另一种推荐算法所采用。<br>　　5）层叠（Cascade）：先用一种推荐技术产生一种粗糙的推荐结果，第二种推荐技术在此推荐结果的基础上进一步作出更精确的推荐。<br>　　6）特征扩充（Feature augmentation）：一种技术产生附加的特征信息嵌入到另一种推荐技术的特征输入中。<br>　　7）元级别（Meta-level）：用一种推荐方法产生的模型作为另一种推荐方法的输入。</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>[1] Wiki. <a href="https://zh.wikipedia.org/wiki/%E5%8D%94%E5%90%8C%E9%81%8E%E6%BF%BE" target="_blank" rel="noopener">协同过滤</a>. 推荐算法. 2019.</p>
<p>[2] 刘建平. <a href="https://www.cnblogs.com/pinard/p/6349233.html" target="_blank" rel="noopener">协同过滤推荐算法总结</a>. 机器学习. 2017. </p>
<p>[3] 百度百科. <a href="https://baike.baidu.com/item/%E6%8E%A8%E8%8D%90%E7%AE%97%E6%B3%95" target="_blank" rel="noopener">推荐系统</a>. 推荐系统. 2019.</p>
]]></content>
      <categories>
        <category>推荐系统</category>
      </categories>
      <tags>
        <tag>推荐系统</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Promise 对象</title>
    <url>/2019/02/21/Promise%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<p><strong>Promise</strong> 是异步编程的一种解决方案，起初它只存在于各种三方库，后来 ES6 将其写入了语言标准，以原生的方式提供 Promise 对象。目前比较流行的浏览器均支持。用 Promise 有以下优点：1. 分离了异步数据获取和业务逻辑，有利于代码的复用；2. 可以采用链式写法。 但是 Promise 的实现比较复杂，学习有困难。</p>
<a id="more"></a>
<h2 id="使用-Promise"><a href="#使用-Promise" class="headerlink" title="使用 Promise"></a>使用 Promise</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> status = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"some output 1"</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (status) &#123;</span><br><span class="line">        resolve(<span class="string">"SUCCESS"</span>); <span class="comment">// 成功时异步回调</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        reject(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"FAIL"</span>)); <span class="comment">// 失败时异步回调</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"some output 2"</span>);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"some output 3"</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">result</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(result);</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(error);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"some output 4"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果：（当 status = true 时）</span></span><br><span class="line"><span class="comment">// some output 1</span></span><br><span class="line"><span class="comment">// some output 2</span></span><br><span class="line"><span class="comment">// some output 3</span></span><br><span class="line"><span class="comment">// some output 4</span></span><br><span class="line"><span class="comment">// SUCCESS</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果: （当 status = false 时）</span></span><br><span class="line"><span class="comment">// some output 1</span></span><br><span class="line"><span class="comment">// some output 2</span></span><br><span class="line"><span class="comment">// some output 3</span></span><br><span class="line"><span class="comment">// some output 4</span></span><br><span class="line"><span class="comment">// Error: FAIL</span></span><br><span class="line"><span class="comment">// at (index):39</span></span><br><span class="line"><span class="comment">// at new Promise (&lt;anonymous&gt;)</span></span><br><span class="line"><span class="comment">// at window.onload ((index):35)</span></span><br></pre></td></tr></table></figure>
<p>Promise 对象一被创建就会执行，所以 Promise 先输出 “some output 1”，而 resolve() 和 reject() 均属于异步回调，它们都要等脚本中的同步操作执行完毕再执行。<code>promise.then()</code>可以接收两个参数，分别是成功时的回调函数原型（resolve）和失败时的回调函数原型（reject）。</p>
<h2 id="Promise-的三种状态"><a href="#Promise-的三种状态" class="headerlink" title="Promise 的三种状态"></a>Promise 的三种状态</h2><p>Promise 对象有三种状态： Pending（初始化状态）、Fulfilled（执行成功）、Rejected（执行失败）。</p>
<p>这三种状态只能从 Pending 到 Fulfilled 或从 Pending 到 Rejected，而不能由 Fulfilled 转变为 Rejected 或反过来。Promise 中的 then 的回调函数只会被调用一次，所以其状态是不可逆的，要么执行成功回调，要么执行失败回调。</p>
<p><img src="https://i.loli.net/2019/02/22/5c6ecce5c415c.png" alt="Promise 三种状态"></p>
<h2 id="Promise-的链式调用"><a href="#Promise-的链式调用" class="headerlink" title="Promise 的链式调用"></a>Promise 的链式调用</h2><p>Promise 的 then 方法返回一个<strong>新的</strong> Promise，这使得你能够写出像下面这样的链式写法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">promise.then().then()</span><br></pre></td></tr></table></figure>
<p>这就比无限嵌套下去的回调地狱要高明的多。</p>
]]></content>
      <tags>
        <tag>Promise</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript异步编程</title>
    <url>/2019/02/19/JavaScript%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>JavaScript 语言的执行环境是“单线程（Single thread）”的。<strong>线程（Thread）</strong>是操作系统能够进行运算调度的最小单位。一条线程指的是进程中一个单一顺序的控制流。所以，“单线程”表示一次只能完成一件事。如果有多个任务就必须排队。</p>
<a id="more"></a>
<p>这种模式（同步）的好处是实现起来比较简单，执行环境相对单纯；坏处是显然存在死等的可能性，如果前一个任务耗时较长，则后面的任务就必须等待，会拖延整个程序的执行。常见的浏览器无响应往往就是因为某一段 JavaScript 代码长时间运行（比如死循环）而导致整个页面卡在某个地方，其它任务无法执行。</p>
<p>为了解决这个问题，JavaScript 语言将任务的<strong>执行模式</strong>分成了两种：<strong>同步（Syschronous）</strong>和<strong>异步（Asynchronous）</strong></p>
<p>“同步模式”就是指前面的顺序执行，后一个任务等待前一个任务执行结束；“异步模式”则不同，每一个任务有一个或多个回调函数，回调函数后的任务可以不等回调函数执行完毕就可以先执行，程序的执行顺序与代码的先后排列顺序是不一致的。</p>
<p>“异步模式”非常有用。在浏览器端，耗时很长的操作都应该异步执行，避免浏览器失去响应，最好的例子就是 Ajax 操作。</p>
<p>下面是“异步模式”编程的几种方法。</p>
<h2 id="回调函数"><a href="#回调函数" class="headerlink" title="回调函数"></a>回调函数</h2><p><strong>“回调函数”</strong>就是指被作为实参传入另一函数，并在该外部函数内被调用以用来完成某些任务的函数。</p>
<p><strong>“回调函数”可以是同步的也可以是异步的。</strong>而并不是新手所认为的只要是回调函数就一定是“异步模式”。</p>
<h3 id="同步回调"><a href="#同步回调" class="headerlink" title="同步回调"></a>同步回调</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> name = prompt(<span class="string">"请输入你的名字："</span>);</span><br><span class="line">    callback(name);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"main: "</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"callback: "</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">f1(f2);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行结果：(输入 “AlenWalker”)</span></span><br><span class="line"><span class="comment">// callback: AlenWalker</span></span><br><span class="line"><span class="comment">// main: AlenWalker</span></span><br></pre></td></tr></table></figure>
<p>正如结果显示，其任务执行顺序与任务排列顺序一致，表现为同步。prompt -&gt; callback -&gt; console</p>
<h3 id="异步回调"><a href="#异步回调" class="headerlink" title="异步回调"></a>异步回调</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> name = prompt(<span class="string">"请输入你的名字："</span>);</span><br><span class="line">    </span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    	callback(name);</span><br><span class="line">     &#125;, <span class="number">1000</span>); <span class="comment">// setTimeout 具有异步机制</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"main: "</span> + name);</span><br><span class="line">    <span class="comment">// while(true) &#123;&#125; // 进入死循环后，callback 一秒后并不会执行（因为单线程一次只能做一件事）。注意：异步回调函数永远要等主调函数执行完毕，而不是一秒后就会执行</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"callback: "</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行结果：（输入 “AlenWalker”）</span></span><br><span class="line"><span class="comment">// main: AlenWalker</span></span><br><span class="line"><span class="comment">// callback: AlenWalker</span></span><br></pre></td></tr></table></figure>
<p>显然，这里的结果显示 callback 在 console.log(“main: “ + name) 的后面执行，而并非顺序执行，它是异步的。</p>
<p>采用“回调函数”实现“异步模式”的优点是简单、容易理解和部署，缺点是不利于代码的阅读和维护，各个任务之间高度<strong>耦合（Coupling）</strong>，流程较为混乱，而且每个任务只能指定一个回调函数。</p>
<h2 id="事件监听"><a href="#事件监听" class="headerlink" title="事件监听"></a>事件监听</h2><p><strong>事件驱动模式</strong>下任务的执行不取决于代码的顺序，而取决于某个事件是否发生。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// HTML: &lt;button id="btn"&gt;click me&lt;/button&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> btn = <span class="built_in">document</span>.querySelector(<span class="string">"#btn"</span>);</span><br><span class="line"></span><br><span class="line">btn.addEventListener(<span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(<span class="string">"执行了 onclick 事件"</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"我被输出了"</span>); <span class="comment">// 不会一直等待点击事件执行</span></span><br></pre></td></tr></table></figure>
<h2 id="发布-订阅"><a href="#发布-订阅" class="headerlink" title="发布/订阅"></a>发布/订阅</h2><p><strong>发布/订阅模式（publish-subscribe pattern）</strong>又称<strong>观察者模式（observer pattern）</strong>。</p>
<p>引用一下阮一峰老师的例子：我们假定存在一个“信号中心”，某个人物执行完成，就向信号中心发布一个信号，其他任务可以向信号中心订阅这个信号，从而知道自己什么时候可以开始执行。</p>
<p>再举一个比较生动的例子：微信公众号，微信公众号可以“发布”文章，用户可以“订阅”公众号，当然用户也有“取消订阅”公众号的权利。所以“订阅者”也可以取消订阅某个事件，这样就算你发布了这个事件我也不会理会。当然，用户还可以选择获取什么样的文章，所以“发布”是可以传参的。（你可能光看例子会很蒙，建议仔细分析代码后再结合例子推敲。）</p>
<p>事件中心：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> list = &#123;</span><br><span class="line">    key: [callback1, callback2, callback3, ...],</span><br><span class="line">    key: [callback1, callback2, callback3, ...],</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>我们每订阅一条事件就向事件中心添加一条，每取消订阅一次就移除一条，每发布一次信息就执行一条。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> observerClass = &#123;</span><br><span class="line">    list: &#123;&#125;,</span><br><span class="line">    subscribe: <span class="function"><span class="keyword">function</span>(<span class="params">key, callback</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="keyword">this</span>.list[key]) &#123;</span><br><span class="line">            <span class="keyword">this</span>.list[key] = [];</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">this</span>.list[key].push(callback);</span><br><span class="line">    &#125;,</span><br><span class="line">    publish: <span class="function"><span class="keyword">function</span>(<span class="params">key, args</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">const</span> fns = <span class="keyword">this</span>.list[key];</span><br><span class="line">        <span class="keyword">if</span> (!fns || fns.length === <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">let</span> len = fns.length;</span><br><span class="line">        <span class="keyword">while</span>(len--) &#123;</span><br><span class="line">            fns[len].apply(<span class="keyword">this</span>, args);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    unsubscribe: <span class="function"><span class="keyword">function</span>(<span class="params">key, fn</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">const</span> fns = <span class="keyword">this</span>.this.list[key];</span><br><span class="line">        <span class="keyword">if</span> (!fns) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!fn) &#123;</span><br><span class="line">            fns &amp;&amp; (fns.length = <span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">let</span> len = fns.length;</span><br><span class="line">            <span class="keyword">while</span>(len--) &#123;</span><br><span class="line">                <span class="keyword">if</span> (fns[len] === fn) &#123;</span><br><span class="line">                    fns.splice(len, <span class="number">1</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">observerClass.subscribe(<span class="string">"event"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(name);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">observerClass.subscribe(<span class="string">"event"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"twice "</span> + name);</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">observerClass.publish(<span class="string">"event"</span>, [<span class="string">"AlenWalker"</span>]);</span><br></pre></td></tr></table></figure>
<h2 id="Promises-对象"><a href="#Promises-对象" class="headerlink" title="Promises 对象"></a>Promises 对象</h2><p><a href="https://juejin.im/post/5b83cb5ae51d4538cc3ec354" target="_blank" rel="noopener">Promise 实现原理（附源码）</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>异步编程</tag>
      </tags>
  </entry>
  <entry>
    <title>网易云接口加直链下载vip歌曲</title>
    <url>/2019/02/17/%E7%BD%91%E6%98%93%E4%BA%91%E6%8E%A5%E5%8F%A3%E5%8A%A0%E7%9B%B4%E9%93%BE%E4%B8%8B%E8%BD%BDvip%E6%AD%8C%E6%9B%B2/</url>
    <content><![CDATA[<ul>
<li><p>POST请求（歌曲）：<br><a href>http://music.163.com/api/search/get/web?csrf_token=hlpretag=&amp;hlposttag=&amp;s=歌曲名称&amp;type=1&amp;offset=0&amp;total=true&amp;limit=搜索条数</a></p>
</li>
<li><p>GET请求（歌词）：<br><a href>http://music.163.com/api/song/lyric?os=pc&amp;id=歌曲id&amp;lv=-1&amp;kv=-1&amp;tv=-1</a></p>
</li>
<li><p>GET请求（直链，hMusic高品质, mMusic中品质, lMusic低品质）：<br><a href>http://music.163.com/api/song/detail/?id=歌曲id&amp;ids=%5B歌曲id%5D</a></p>
</li>
<li><p>GET请求（直链下载 含VIP）：<br><a href>http://music.163.com/song/media/outer/url?id=歌曲id.mp3</a></p>
</li>
</ul>
]]></content>
      <tags>
        <tag>网易云</tag>
      </tags>
  </entry>
  <entry>
    <title>遗传算法之二进制编码</title>
    <url>/2019/02/16/%E9%81%97%E4%BC%A0%E7%AE%97%E6%B3%95%E4%B9%8B%E4%BA%8C%E8%BF%9B%E5%88%B6%E7%BC%96%E7%A0%81/</url>
    <content><![CDATA[<h1 id="遗传算法的基本步骤"><a href="#遗传算法的基本步骤" class="headerlink" title="遗传算法的基本步骤"></a>遗传算法的基本步骤</h1><p>遗传算法 GA 的流程如图所示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">flowchat GA</span><br><span class="line">    st: 开始</span><br><span class="line">    code: 编码和产生初始群体</span><br><span class="line">    fitness: 计算各个个体的适应度值</span><br><span class="line">    is: 是否满足算法收敛准则？</span><br><span class="line">    result: 输出收敛结果</span><br><span class="line">    choose: 执行选择操作</span><br><span class="line">    cross: 执行交叉操作</span><br><span class="line">    variation: 执行变异操作</span><br><span class="line">    end: 结束</span><br><span class="line">    st&gt;code&gt;fitness&gt;is</span><br><span class="line">    is(y)&gt;choose&gt;cross&gt;variation&gt;end</span><br><span class="line">    is(n)&gt;result</span><br><span class="line">    variation&gt;fitness</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<h2 id="编码"><a href="#编码" class="headerlink" title="编码"></a>编码</h2><p>把所需要选择的特征进行编号，每一个特征就是一个基因，一个解就是一串基因的组合。为了减少组合数量，在图像中进行分块，然后把每一块看成一个基因进行组合优化的计算。每个解得基因数量是要通过实验确定的。</p>
<p>遗传算法不能直接处理问题空间的参数，必须把它们转换成遗传空间的由基因按一定结构组成的染色体或个体。这一转换操作就叫做编码。评估编码策略常采用以下 3 个规范。<br><strong>（1） 完备性（Completeness）：</strong>问题空间中的所有点（候选解）都能作为 GA 空间中的点（染色体）表现。<br><strong>（2） 健全性（Soundness）：</strong>GA 空间中的染色体能对应所有空间中的候选解。<br><strong>（3） 非冗余性（Nonredundancy）：</strong>染色体和候选解一一对应。</p>
<p>目前几种常用的编码技术有<strong>二进制编码、浮点数编码、字符编码、编程编码等</strong>。<strong>二进制编码</strong>是遗传算法中最常见的编码方法，即由二进制字符集 {0， 1} 产生通常的 0, 1 字符串来表示问题的候选解。它具有以下<strong>特点</strong>：<br>（1） 简单易行；<br>（2） 符合最小字符集编码原则；<br>（3） 便于用模式定理进行分析。</p>
<p><strong><em>染色体编码最常用的是二进制编码，对于离散性变量直接进行编码，对于连续性变量先离散化后再编码。</em></strong></p>
<p>科普1：SPSS常用的基础操作（2）——连续变量离散化<br>（下面的这个地址中详细介绍了什么是连续变量离散化及其必要性）</p>
<pre><code>人人都是数据咖：http://www.ppvke.com/Blog/archives/44271
</code></pre><p>科普2：连续特征的离散化：在什么情况下将连续的特征离散化后可以获得更好的效果？</p>
<pre><code>问答来源于知乎：https://www.zhihu.com/question/31989952
</code></pre><h2 id="举个例子"><a href="#举个例子" class="headerlink" title="举个例子"></a>举个例子</h2><p>已知一元函数：</p>
<p><strong>F(x) = xsin(10pi*x)+2      x∈[-1, 2]</strong></p>
<p>现在要求在既定的区间内找出函数的最大值。</p>
<p>首先我们可以先用 MATLAB 把该函数的图像画出来：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">clc, clear;</span><br><span class="line">syms f(x); <span class="comment">% 声明函数</span></span><br><span class="line">x = <span class="built_in">linspace</span>(<span class="number">-1</span>,<span class="number">2</span>,<span class="number">3000</span>); <span class="comment">% 定义 x, x 属于 [-1, 2]</span></span><br><span class="line">f = x.*<span class="built_in">sin</span>(<span class="number">10</span>*<span class="built_in">pi</span>.*x)+<span class="number">2</span>; <span class="comment">% 定义函数，因为 x 是向量，所以采用点乘</span></span><br><span class="line"><span class="built_in">plot</span>(f); <span class="comment">% 画图</span></span><br></pre></td></tr></table></figure>
<p>然后做出图形如下：</p>
<p>￼￼￼￼<img src="http://img.blog.csdn.net/20170905193050633?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQveDEwMjA5MTUwOTg=/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast" alt="这里写图片描述"></p>
<p>我们可以看到，该图形显示说明该函数具有多个局部最优解，所以适合用遗传算法进行求解。</p>
<p>由遗传算法的基本步骤可知我们第一步应该是编码：</p>
<p><strong>二进制编码</strong></p>
<p>受到人类染色体结构的启发，我们可以设想一下，假设目前只有 0 和 1 两种碱基，我们也用一条链把它们有序的串连在一起，因为每一个单位都能表现出 1bit 的信息量，所以一条足够长的染色体就能为我们勾勒出一个个体特征的所有特征。<strong>这就是二进制编码</strong></p>
<p>下面将介绍如何建立二进制编码到一个实数的映射。</p>
<p>明显地，一定长度的二进制编码序列，只能表示一定精度的浮点数。譬如我们要求小数点后<strong>精确到六位小数</strong>，由于区间长度为</p>
<pre><code>2 - (-1) = 3
</code></pre><p>为了保证精度的要求，至少把区间 [-1, 2] 分为 3*10^6 等份。又因为</p>
<pre><code>2097152=2^21 &lt; 3*10^6 &lt; 2^22=4194304
</code></pre><p>所以编码的二进制串至少有 22 位。<br>把一个二进制串 （b1b2…bn） 转换为区间里面对应的实数值通过下面两个步骤。<br>（1） 将一个二进制串代表的二进制转换为十进制数：</p>
<p><img src="http://img.blog.csdn.net/20170905193137367?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQveDEwMjA5MTUwOTg=/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast" alt="这里写图片描述"><br>（2） 对应区间的实数：</p>
<p><img src="http://img.blog.csdn.net/20170905193200424?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQveDEwMjA5MTUwOTg=/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast" alt="这里写图片描述"></p>
<p>或许有人并不知道是如何把数值转换到对应区间的实数的，为什么要这么做？我也是问了一下我的小伙伴才知晓的，下面我来简单的说一下：<br>通常我们归一化到 [0, 1]，有时候我们需要归一化到其它区间，这样算一下就可以找到数列的最小值 m 及最大值 M，如果指定的区间是 [a, b]，即：</p>
<pre><code>m--&gt;a, M--&gt;b；
</code></pre><p>系数为：</p>
<pre><code>k = (b-a)/(M-m)
</code></pre><p>对任意项Xn：变成：</p>
<pre><code>X = a+k(Xn-m)
</code></pre><p>（二进制编码）<br>将十进制转换为二进制的 MATLAB 程序代码如下：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 将十进制数转换为二进制数（二进制编码）</span></span><br><span class="line"><span class="comment">% @params dec_num 十进制数</span></span><br><span class="line"><span class="comment">% @params N       需保留的二进制小数位数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bin_num</span> = <span class="title">encode</span><span class="params">(dec_num, N)</span></span></span><br><span class="line">	split = <span class="string">'.'</span>;</span><br><span class="line">	<span class="keyword">if</span> <span class="built_in">rem</span>(dec_num, <span class="number">1</span>) == <span class="number">0</span></span><br><span class="line">		bin_num = dec2bin(dec_num);</span><br><span class="line">		float_num = <span class="built_in">zeros</span>(<span class="number">1</span>, N);</span><br><span class="line">        bin_num = strcat(num2str(bin_num), split);</span><br><span class="line">        bin_num = strcat(bin_num, num2str(float_num));</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		remainder = <span class="built_in">rem</span>(dec_num, <span class="number">1</span>); <span class="comment">% 小数部分</span></span><br><span class="line">		integer = <span class="built_in">floor</span>(dec_num); <span class="comment">% 整数部分</span></span><br><span class="line">		bin_num_int = dec2bin(integer);</span><br><span class="line">		<span class="built_in">i</span> = <span class="number">1</span>;</span><br><span class="line">		flag = <span class="built_in">true</span>;</span><br><span class="line">		<span class="keyword">while</span>(flag == <span class="built_in">true</span>)</span><br><span class="line">            remainder = remainder*<span class="number">2</span>;</span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">i</span> &gt; N) <span class="comment">% 是否满足精度</span></span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			<span class="keyword">end</span></span><br><span class="line">			<span class="keyword">if</span> remainder &gt; <span class="number">1</span></span><br><span class="line">				record(<span class="built_in">i</span>) = <span class="number">1</span>;</span><br><span class="line">                remainder = <span class="built_in">rem</span>(remainder, <span class="number">1</span>);</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> remainder == <span class="number">1</span></span><br><span class="line">				record(<span class="built_in">i</span>) = <span class="number">1</span>;</span><br><span class="line">				remainder = <span class="built_in">rem</span>(remainder, <span class="number">1</span>);</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				record(<span class="built_in">i</span>) = <span class="number">0</span>;</span><br><span class="line">			<span class="keyword">end</span></span><br><span class="line">			<span class="built_in">i</span> = <span class="built_in">i</span>+<span class="number">1</span>;</span><br><span class="line">		<span class="keyword">end</span></span><br><span class="line">		bin_num_flt = record;</span><br><span class="line">		bin_num = strcat(num2str(bin_num_int), split);</span><br><span class="line">		bin_num = strcat(bin_num, num2str(bin_num_flt));</span><br><span class="line">        <span class="keyword">end</span></span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>遗传算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Java无边框窗体透明化</title>
    <url>/2019/02/16/Java%E6%97%A0%E8%BE%B9%E6%A1%86%E7%AA%97%E4%BD%93%E9%80%8F%E6%98%8E%E5%8C%96/</url>
    <content><![CDATA[<p>这几天我在写一个记事本程序，为了方便记事本记事，我准备将记事本界面透明化。于是上网查了一下资料。然后将透明化界面做了出来。</p>
<p>可是在做的过程中我还是遇到了一些小小的问题，所以就把它记录了下来。</p>
<a id="more"></a>
<h2 id="问题1：奇怪的错误-访问限制-Access-restriction"><a href="#问题1：奇怪的错误-访问限制-Access-restriction" class="headerlink" title="问题1：奇怪的错误(访问限制:Access restriction)"></a>问题1：奇怪的错误(访问限制:Access restriction)</h2><p>透明化窗体中有这样一段代码:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 判断系统是否支持窗体透明化</span></span><br><span class="line"><span class="keyword">if</span> (com.sun.awt.AWTUtilities.isWindowOpaque(<span class="keyword">this</span>)) &#123;</span><br><span class="line">	<span class="comment">// 设置窗体透明化, 透明参数值[0, 1](float类型)</span></span><br><span class="line">	com.sun.awt.AWTUtilities.setWindowOpacty(<span class="keyword">this</span>, <span class="number">0.5f</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">	JOptionPane.showMessageDialog(<span class="keyword">this</span>, <span class="string">"系统不支持 JDK版本过低或JRE系统库缺损"</span>);</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<p>这段代码明明是没有错的，但是Eclipse却偏偏报了错，这是为什么呢？<br>通过错误提示，点击 “Configure problem severity” ，我发现了问题所在，原来是eclipse的配置问题。</p>
<h3 id="解决方法如下"><a href="#解决方法如下" class="headerlink" title="解决方法如下:"></a>解决方法如下:</h3><p>菜单栏找到 Window -&gt; Preferences -&gt; Java Compiler(中间可能省略几步，你可以搜索) -&gt; Errors/Warnings -&gt; Deprecated and restricted API -&gt; Forbidden reference(access rules) 后面的 Error 下拉改成 Warning 或者 Ignore -&gt; 确定。<br>然后就 OK 了!</p>
<h2 id="问题二：郁闷的异常-IllegalComponentStateException"><a href="#问题二：郁闷的异常-IllegalComponentStateException" class="headerlink" title="问题二：郁闷的异常(IllegalComponentStateException)"></a>问题二：郁闷的异常(IllegalComponentStateException)</h2><p>当我所有的代码都写完之后，一运行，结果出现了编译异常。当时我想把它抛出去，但是我又是遇到问题不能放的人。所以就找了一下原因。</p>
<pre><code>Exception in thread &quot;main&quot; java.awt.IllegalComponentStateException: The frame is decorated
at java.awt.Frame.setOpacity(Unknown Source)
at java.awt.Window$1.setOpacity(Unknown Source)
at com.sun.awt.AWTUtilities.setWindowOpacity(Unknown Source)
at Opacity.&lt;init&gt;(Opacity.java:18)
at Opacity.main(Opacity.java:7)
</code></pre><p>最后我终于发现了错误的原因，原来是由于我写的窗体是可显示的所以抛出了这样一个异常。</p>
<p>之后我在我的源代码之上加了这么一句就好了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">this</span>.setUndecorated(<span class="keyword">true</span>);  <span class="comment">// 禁用窗体装饰,加在前面就好了</span></span><br></pre></td></tr></table></figure>
<p>关于这个函数我在 《JDK API 1.6.0中文版》查了一下，上面是这样说的。</p>
<blockquote>
<p>setUndecorated<br>public void setUndecorated(boolean undecorated)禁用或启用此窗体的装饰。只有在窗体不可显示时才调用此方法。 </p>
<p>参数：<br>undecorated - 如果没有启用窗体装饰，则为 true；如果启用了窗体装饰，则为 false。<br>抛出：<br>IllegalComponentStateException - 如果窗体是可显示的。<br>从以下版本开始：<br>1.4<br>另请参见：<br>isUndecorated(), Component.isDisplayable(), JFrame.setDefaultLookAndFeelDecorated(boolean)</p>
</blockquote>
<p>测试源码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.swing.JButton;</span><br><span class="line"><span class="keyword">import</span> javax.swing.JFrame;</span><br><span class="line"><span class="keyword">import</span> javax.swing.JOptionPane;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Opacity</span> <span class="keyword">extends</span> <span class="title">JFrame</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">new</span> Opacity();</span><br><span class="line">	&#125;</span><br><span class="line">	Opacity() &#123;</span><br><span class="line">		<span class="keyword">this</span>.setTitle(<span class="string">"窗体透明化测试"</span>);</span><br><span class="line">		</span><br><span class="line">		JButton btn = <span class="keyword">new</span> JButton();</span><br><span class="line">		<span class="keyword">this</span>.add(btn);</span><br><span class="line">		<span class="keyword">double</span> value = <span class="number">0.4</span>;</span><br><span class="line">		<span class="comment">// 判断是否支持透明度</span></span><br><span class="line">		<span class="keyword">this</span>.setUndecorated(<span class="keyword">true</span>); <span class="comment">// 禁用或启用此窗体的修饰。只有在窗体不可显示时</span></span><br><span class="line">								   <span class="comment">//才调用此方法。</span></span><br><span class="line">		<span class="keyword">if</span> (com.sun.awt.AWTUtilities.isWindowOpaque(<span class="keyword">this</span>)) &#123;</span><br><span class="line">			com.sun.awt.AWTUtilities.setWindowOpacity(<span class="keyword">this</span>, (<span class="keyword">float</span>)(<span class="number">1</span> - value));</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			JOptionPane.showMessageDialog(<span class="keyword">this</span>, <span class="string">"系统不支持 JDK版本过低或 JRE 系统库缺损"</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">this</span>.setBounds(<span class="number">400</span>,<span class="number">400</span>,<span class="number">400</span>,<span class="number">300</span>);</span><br><span class="line">		<span class="keyword">this</span>.setDefaultCloseOperation(<span class="keyword">this</span>.EXIT_ON_CLOSE);</span><br><span class="line">		<span class="keyword">this</span>.setVisible(<span class="keyword">true</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>Matlab 求解数学问题</title>
    <url>/2019/01/16/Matlab%E6%B1%82%E8%A7%A3%E6%95%B0%E5%AD%A6%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p><strong>如果看完之后还是对相关函数的运用不甚了解，请务必使用 help 查看更详细的帮助文档！</strong></p>
<a id="more"></a>
<h3 id="求解一元-n-次方程"><a href="#求解一元-n-次方程" class="headerlink" title="求解一元 n 次方程"></a>求解一元 n 次方程</h3><p>例：求解一元二次方程 <strong>x^2+2*x+1 = 0</strong></p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数</span></span><br><span class="line">syms f(x);</span><br><span class="line">f = x^<span class="number">2</span> + <span class="number">2</span>*x +<span class="number">1</span>;</span><br><span class="line"><span class="comment">% 利用 solve 函数求解方程</span></span><br><span class="line">x = solve(f);</span><br><span class="line"><span class="comment">% 显示结果</span></span><br><span class="line"><span class="built_in">disp</span>(x);</span><br></pre></td></tr></table></figure>
<h3 id="求解方程组"><a href="#求解方程组" class="headerlink" title="求解方程组"></a>求解方程组</h3><p>例：求二元一次方程组<br><strong><br>9x + 8y = 10    ①<br>13x + 14y = 12    ②
</strong></p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解多元方程组</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数</span></span><br><span class="line">syms f1(x,y) f2(x,y);</span><br><span class="line">f1 = <span class="number">9</span>*x + <span class="number">8</span>*y - <span class="number">10</span>;</span><br><span class="line">f2 = <span class="number">13</span>*x + <span class="number">14</span>*y - <span class="number">12</span>;</span><br><span class="line"><span class="comment">% 求解方程组</span></span><br><span class="line">[x, y] = solve(f1, f2)</span><br></pre></td></tr></table></figure>
<p>例：求非线性方程组<br><strong><br>x^2 + y^2 = 10    ①<br>2x + 3y = 0    ②
</strong></p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解多元方程</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数</span></span><br><span class="line">syms f1(x,y) f2(x,y);</span><br><span class="line">f1 = x^<span class="number">2</span> + y^<span class="number">2</span> - <span class="number">10</span>;</span><br><span class="line">f2 = <span class="number">2</span>*x + <span class="number">3</span>*y;</span><br><span class="line"><span class="comment">% 求解方程组</span></span><br><span class="line">[x, y] = solve(f1, f2)</span><br></pre></td></tr></table></figure>
<p>求出来的结果是两个点的坐标，x 为横坐标， y为纵坐标。</p>
<h3 id="求解符号解"><a href="#求解符号解" class="headerlink" title="求解符号解"></a>求解符号解</h3><p>求解符号解只需要我们利用 <strong>syms</strong> 函数在方程函数中声明符号变量就好了。<br>例：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解符号方程</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数及变量 a, b, c </span></span><br><span class="line">syms f1(x,y) f2(x,y) a b c;</span><br><span class="line">f1 = a*x^<span class="number">2</span> + b*y^<span class="number">2</span> + c;</span><br><span class="line">f2 = a*x + b*y + c;</span><br><span class="line"><span class="comment">% 求解方程组</span></span><br><span class="line">[x, y] = solve(f1, f2)</span><br></pre></td></tr></table></figure>
<p>解得的结果就是带 a, b, c 三个符号变量的符号解：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">x =</span><br><span class="line"> </span><br><span class="line"> -(c - (b*(c - (-(a*c*(a + b + c))/b)^(<span class="number">1</span>/<span class="number">2</span>)))/(a + b))/a</span><br><span class="line"> -(c - (b*(c + (-(a*c*(a + b + c))/b)^(<span class="number">1</span>/<span class="number">2</span>)))/(a + b))/a</span><br></pre></td></tr></table></figure>
<p>​<br><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">y =</span><br><span class="line"> </span><br><span class="line"> -(c - (-(a*c*(a + b + c))/b)^(<span class="number">1</span>/<span class="number">2</span>))/(a + b)</span><br><span class="line"> -(c + (-(a*c*(a + b + c))/b)^(<span class="number">1</span>/<span class="number">2</span>))/(a + b)</span><br></pre></td></tr></table></figure></p>
<h3 id="函数求导"><a href="#函数求导" class="headerlink" title="函数求导"></a>函数求导</h3><figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 函数求导</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数</span></span><br><span class="line">syms y(x);</span><br><span class="line">y = <span class="number">2</span> * x^<span class="number">2</span>;</span><br><span class="line"><span class="comment">% 求函数的导</span></span><br><span class="line">Dy = diff(y); <span class="comment">% 求函数的一阶导</span></span><br><span class="line">D2y = diff(y, <span class="number">2</span>); <span class="comment">% 求函数的二阶导</span></span><br></pre></td></tr></table></figure>
<h3 id="求解微分方程"><a href="#求解微分方程" class="headerlink" title="求解微分方程"></a>求解微分方程</h3><p>下面的例子为输入 <strong>help dsolve</strong> 命令后，帮助文档中所给出的例子。</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解常微分方程</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数及变量</span></span><br><span class="line">syms y(x) a;</span><br><span class="line"><span class="comment">% 函数微分方程</span></span><br><span class="line">Dy = diff(y); <span class="comment">% 求 y 的一阶导</span></span><br><span class="line">D2y = diff(y, <span class="number">2</span>); <span class="comment">% 求 y 的二阶导</span></span><br><span class="line"><span class="comment">%求解微分方程</span></span><br><span class="line">dsolve(D2y == -a^<span class="number">2</span>*y, y(<span class="number">0</span>) == <span class="number">1</span>, Dy(<span class="built_in">pi</span>/a) == <span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p><strong>注意：</strong> <strong>dsolve</strong> 求解微分方程的时候，如果里面求的为 <strong>n</strong> 阶导，那么后面对应给 <strong>n</strong> 个等式。（另外：是 <strong>‘==’</strong> 而不是 <strong>‘=’</strong> ，写的时候需要注意！）</p>
<h3 id="求解不定积分"><a href="#求解不定积分" class="headerlink" title="求解不定积分"></a>求解不定积分</h3><p>使用 <strong>int</strong> 函数求解不定积分：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解不定积分</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数及常数 C</span></span><br><span class="line">syms f(x) C;</span><br><span class="line">f = <span class="built_in">sin</span>(x)/x;</span><br><span class="line"><span class="comment">% 求解不定积分</span></span><br><span class="line">int(f) + C</span><br></pre></td></tr></table></figure>
<p>运行脚本后得到如下的结果：</p>
<pre><code>ans =

C + sinint(x)
</code></pre><p>其中，<strong>sinint()</strong> 为正弦积分函数，返回 <strong>sin(x)/x</strong> 在区间 [0, x]之间的定积分，不是初等函数。</p>
<h3 id="求解定积分"><a href="#求解定积分" class="headerlink" title="求解定积分"></a>求解定积分</h3><p>例：求函数 <strong>x^2*e^x</strong> 在 (0,1) 上的积分。</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求解定积分</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义函数</span></span><br><span class="line">syms f(x);</span><br><span class="line">f = x^<span class="number">2</span>*<span class="built_in">exp</span>(x);</span><br><span class="line"><span class="comment">% 求解函数在 0 到 1 上的定积分</span></span><br><span class="line">int(f, <span class="number">0</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<h3 id="结果化简及结果的美化"><a href="#结果化简及结果的美化" class="headerlink" title="结果化简及结果的美化"></a>结果化简及结果的美化</h3><p>使用 <strong>simplify()</strong> 函数可以对结果进行化简。而使用 <strong>pretty()</strong> 函数可以把得到的结果以相对美观的方式输出。</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 方程式的化简</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义变量</span></span><br><span class="line">syms a b;</span><br><span class="line">f = <span class="number">2</span>*a + <span class="number">4</span>*b</span><br><span class="line"><span class="comment">% 化简</span></span><br><span class="line">simplify(f)</span><br></pre></td></tr></table></figure>
<hr>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 方程的美化</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义变量</span></span><br><span class="line">syms a b;</span><br><span class="line">f = <span class="built_in">sqrt</span>(a^<span class="number">2</span>+b^<span class="number">2</span>)/b</span><br><span class="line"><span class="comment">% 美化结果</span></span><br><span class="line">pretty(f)</span><br></pre></td></tr></table></figure>
<h3 id="符号替换"><a href="#符号替换" class="headerlink" title="符号替换"></a>符号替换</h3><p>当我们要给一个方程中的变量赋值的时候，我们可以采用 <strong>subs()</strong> 函数进行符号替换。当然我们也可以把其中的一个或一串字符替换成另一串字符。</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 符号替换</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义变量</span></span><br><span class="line">syms a b;</span><br><span class="line">f = a + b;</span><br><span class="line"><span class="comment">% 给 a, b 赋值</span></span><br><span class="line">a = <span class="number">2</span>; b = <span class="number">4</span>;</span><br><span class="line"><span class="comment">% 变量替换</span></span><br><span class="line">f2 = subs(f, &#123;<span class="string">'a'</span>, <span class="string">'b'</span>&#125;, &#123;a, b&#125;)</span><br></pre></td></tr></table></figure>
<h3 id="求矩阵的行最简及求矩阵的秩"><a href="#求矩阵的行最简及求矩阵的秩" class="headerlink" title="求矩阵的行最简及求矩阵的秩"></a>求矩阵的行最简及求矩阵的秩</h3><p>求矩阵的行最简可以使用 <strong>rref</strong> 函数；求矩阵的秩可以使用 <strong>rank</strong> 函数。</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">%% 求矩阵 A 的行最简</span></span><br><span class="line">clc, clear;</span><br><span class="line"><span class="comment">% 定义矩阵</span></span><br><span class="line">A = [<span class="number">1</span> <span class="number">1</span> <span class="number">-3</span> <span class="number">-1</span>; <span class="number">3</span> <span class="number">-1</span> <span class="number">-3</span> <span class="number">4</span>; <span class="number">1</span> <span class="number">5</span> <span class="number">-9</span> <span class="number">-8</span>];</span><br><span class="line"><span class="comment">% 指定有理式格式</span></span><br><span class="line">format <span class="built_in">rat</span></span><br><span class="line"><span class="comment">% 求矩阵 A 的行最简</span></span><br><span class="line">S = rref(A)</span><br><span class="line"><span class="comment">% 求矩阵 A 的秩</span></span><br><span class="line">rank = rank(A)</span><br></pre></td></tr></table></figure>
<h3 id="齐次线性方程组的通解"><a href="#齐次线性方程组的通解" class="headerlink" title="齐次线性方程组的通解"></a>齐次线性方程组的通解</h3><p>在 MATLAB 中， 函数 <strong>null</strong> 用来求解零空间，即满足 <strong>A·X = 0</strong> 的解空间，实际上是求出解空间的一组基。<br>格式：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">z = null; <span class="comment">% z 的列向量为方程组的正交规范基， 满足 Z' x Z = I</span></span><br><span class="line">z = null(A, <span class="string">'r'</span>); <span class="comment">% z 的列向量是方程 A·X = 0 的有理基</span></span><br></pre></td></tr></table></figure>
<p>例：求解下列方程组的通解<br>￼</p>
<p>matlab 求解程序代码如下：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">A = [<span class="number">1</span> <span class="number">2</span> <span class="number">2</span> <span class="number">1</span>; <span class="number">2</span> <span class="number">1</span> <span class="number">-2</span> <span class="number">-2</span>; <span class="number">1</span> <span class="number">-1</span> <span class="number">-4</span> <span class="number">-3</span>];  <span class="comment">% 原始系数矩阵</span></span><br><span class="line">format <span class="built_in">rat</span>;  <span class="comment">% 指定有理式格式</span></span><br><span class="line">B = null(A, <span class="string">'r'</span>);  <span class="comment">% 求解空间有理基</span></span><br><span class="line"><span class="built_in">disp</span>(B);</span><br></pre></td></tr></table></figure>
<p>结果如下：</p>
<pre><code> 2              5/3     
-2             -4/3     
 1              0       
 0              1       
</code></pre><p>即：</p>
<pre><code>2k1 + (5/3) k2  % 通解1
-2k1 + (-4/3) k2 % 通解2
k1
k2
</code></pre><p>我们也可以通过最简行得到基：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">B = rref(A)  <span class="comment">% 求 A 的最简行</span></span><br></pre></td></tr></table></figure>
<p>则相应的写出线性方程组的通解：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 利用求解空间的有理基继续运算    </span></span><br><span class="line">syms k1 k2;  <span class="comment">% 定义字符变量</span></span><br><span class="line">X = k1*B(:, <span class="number">1</span>) + k2*B(:, <span class="number">2</span>)  <span class="comment">% 得到方程组的通解</span></span><br></pre></td></tr></table></figure>
<h3 id="非齐次线性方程组的通解"><a href="#非齐次线性方程组的通解" class="headerlink" title="非齐次线性方程组的通解"></a>非齐次线性方程组的通解</h3><p>需要先判断非齐次线性方程组是否有解，若有解，然后求通解，步骤如下。<br>Step1：判断 A·X = b 是否有解，若有解，则进行第二步，否则求解终止；<br>Step2：求 A·X = b 的一个特解；<br>Step3：求 A·X = 0 的通解<br>Step4：求 A·X = b 的通解等于 A·X = 0 的通解加上 A·X = b 的一个特解。</p>
<p>例：求解方程组</p>
<p>￼</p>
<p>在 MATLAB 中建立脚本 M 文件：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">clc, clear;    </span><br><span class="line">A = [<span class="number">1</span> <span class="number">-2</span> <span class="number">3</span> <span class="number">-1</span>; <span class="number">3</span> <span class="number">-1</span> <span class="number">5</span> <span class="number">-3</span>; <span class="number">2</span> <span class="number">1</span> <span class="number">2</span> <span class="number">-2</span>];  <span class="comment">% 原始系数矩阵</span></span><br><span class="line">b = [<span class="number">1</span> <span class="number">2</span> <span class="number">3</span>]';  <span class="comment">% 解析矩阵</span></span><br><span class="line">B = [A b];  <span class="comment">% 增广矩阵</span></span><br><span class="line">n = <span class="number">4</span>;</span><br><span class="line">RA = rank(A)  <span class="comment">% 求 A 的秩</span></span><br><span class="line">RB = rank(B)  <span class="comment">% 求 B 的秩</span></span><br><span class="line">format <span class="built_in">rat</span>  <span class="comment">% 指定有理式格式</span></span><br><span class="line"><span class="keyword">if</span> RA==RB &amp;&amp; A==n  <span class="comment">% 判断是否有唯一解</span></span><br><span class="line">    X = A \ b</span><br><span class="line"><span class="keyword">elseif</span> RA==RB &amp;&amp; RA&lt;n  <span class="comment">% 判断是否有无穷解</span></span><br><span class="line">    X = A \ b  <span class="comment">% 求特解</span></span><br><span class="line">    C = null(A, <span class="string">'r'</span>)  <span class="comment">% 求 AX=0 的基础解系</span></span><br><span class="line"><span class="keyword">else</span> X=<span class="string">'equition no solve'</span>  <span class="comment">% 判断无解</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<p>运行后的结果为：</p>
<pre><code>RA =
       2       
RB =
       3       
X =
equition no solve
</code></pre>]]></content>
      <tags>
        <tag>matlab</tag>
      </tags>
  </entry>
</search>
