---
title: 前端代码开发规范
date: 2026-01-30
---

统一编码风格、普及最佳实践、提升代码质量、降低维护成本。

---

## 文件夹命名

使用 `kebab-case` (短横线命名) 命名。

## 组件命名

使用 `PascalCase` (大驼峰命名) 命名。

## 离线引入三方库

将三方库放入 `vendor` 文件夹，然后使用 `npm install ./vendor/xlsx-0.20.3.tgz` 命令引入。
